@startuml diagrama_UC09

|Usuário|
start
if (Deseja facilitar busca?) then (sim)
  :Utilizar ordenação/filtro;
endif
:Localizar item (Nota, Personagem ou Diagrama);
:Clicar em "Excluir";

|Sistema|
if (Item possui vínculos relevantes?) then (sim)
  :Exibir impacto da exclusão;
endif
:Exibir alerta de confirmação irreversível;

|Usuário|
if (Confirma exclusão?) then (sim)
  |Sistema|
  :Remover registro do banco de dados;
  :Atualizar interface;
else (não)
  |Sistema|
  :Cancelar exclusão;
endif

stop
@enduml
